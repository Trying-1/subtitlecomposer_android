[I want create a simple typography subtitle composer video editor, when I import audio and its ass or json file of the  transcribed subtitle. understood, we will use native openGL with kotlin for the processing, and flutter for the frontend understood. a basic NLE editor for multi track editor, got it. ]

[good, now lets improve th UI, with more controls and export a video feature. keep things organize try to have seprate files rather than putting it inline in single file. 
got it]

[add font size, and text postion control ]
[playhead, timeline zoom and pan]
[export to public directory, not the app storage. ]
[move the export button to top, use simple icons instead of big button for the audio, subtitle, export buttons, ]
[I want to create kinetic typography editor, impmement those feature that are need for it, UI, processor, whatever is need to achieve that., understood. ]
[add controller at the bottom. for the fontselector, font size, subtitle text position. animations. etc as horizontall scrollable tabs understood]
[also there issue that the audio is not there in the ouput video.]
[use poppin fonts for the UI, small font size, tiny text for UI, understood, a centralize theme config. ]
[improve the control panel UI by  making the sliders full width, instead of keeping them in same row. ]
[also keep the control panel at the bottom, open the tabs  above the panel got it]
[change the tab menu design to use single colum, for the icon and the text, use micro text for this. got it, like compact tab menu]
[allow to control the postion of text by dragging the text in the preview canvas]
[pinch zoom in and zoom out in the preview canvas. ]
[play head to selected segment. ]
[text color control, and other related to it as well]
[move the top import export button to bottom control panel, and keep the top clean to have more space for the preview.]
[allow  multisegment selection so that we can apply edit to multiple segment]
[cool but UX is that I have long press every time, for multi select instead add   multi mode toggle and select all toggle in the zoom panel , then this will better UX. ]
[lets add text affects control now. understood]
[good, next now allow to drag the segment to new track in the timeline so from now we can have multitrack timeline. understood(common issue gesture conflict with scroll and drag)]
[good now it moving, but while draging down, don't it in horizontally, just change the track, not the time. understood. ]
[good, next no we  allow to extend there there edges, by draging their left or right end to extend them in timeline, understood. but no overlap of segments in same track, if collision happens, move to new track, understood]
[good,but the edge drags is not getting proper drag, like hit-testing for draging the edge is not good enough. even though I am trying to drag the edge, timeline gets scrolled, whats the solution to this]
[yes now its working, alright now next this the core feature of the app,  add a small button(stack) left to the select all switch, what this will do is .when we select multiple sequential segment in a track, like seg1, seg2,seg3, seg4. and then press this button, it move the seg1 to track4, seg2 to track3, seg3 to track2, and seg4 remains in track1, with the same initial time as earlier but with new end time same as the seg4's end time,so like this will a stair stack,  understood. ]
[good, next improvement in the stacking, that when we press it, align selected text in a column in the preview, like seg1 at top, seg2 below it, and so on, understood. ]
[good, also allow to select text directly in the preview canvas, understood, so that we can select any text directly from the preview window, got it]
[good, next allow direct select and drag as well in the preview window, like right now I want to directly drag the text position but it don't allow it like that, I first had to select the text, so to improve the users experience allow direct selection and drag, in preview.  got it]
[cool, now next lets add apect ratio and background control panel.]


[for the background image picker use the image picker, in which instead of file browser a bottom sheets open up.  understood]
[make the timeline control 2 row layout, and also lets put the play pause control inside this, this was we will have more space for the preview window]
[in the upper timeline control panel add a full screen icon button,  what this will do is collapse the timeline control panel and timeline track, making the preview window full screen, so that we can edit things on it easily, udnerstood]
[good, now during this full screen mode, when we swipe the lower area of the preview window allow seek, so what we can effiently edit,  got it. ]
[good, now add the visual feedback for this swipe, like the progess bar of where the current play head is, so this indicator bwetween the bottom control panel and the timeline control panel, unerstood,  like this will kind of % based indicator not ther acutal play head, we have in the timeline ruler, got it]
[cool, next man improve the project tab layout, move aspect ratio as separate control panel tab. use small square buttons for import export button instead of full width buttons]
[okay, next make subtab within the background tab, for soid color and bg image tab got it]
[in the image tab add image position controls. right we don't have that.]

==============version3==================
[good but the those subtab design are not good, make it like full width tab divide in two. got it]
[lets refactor the bottom control panel control tabs, have separate file for each tab, and wire it to bottom control panel, instead of bloating it with huge inline code in it. understood. ]
[below the rotation slider add quick rotation button like +-90, +-180,and +- 15 increament and decrement understood]
[right now we have timeline for the text segment , to improve the app more we need seprate timeline for overlays. similar to the text segment timeline. but below the text timeline, understood, add separate file to implement this and wire it. this might a complex but easy, similar to the text but instead of text we will be using image overlays that we will implement later]
[good next now lets add a overlay control tab in the bottom control panel to add overlays. understood]
[overlay should be added to the playhead, and should be visible like the text segement of text timeline, by default add overlay for 1s duration, and we can drag and adjust them just like the text segments, understood]
[allow drag adjust  for position for the overlay as well in the preview canvas ]
[damn cool, now inside the overlay tab lets add my assets sub-tab, from where we can directly add overlays instead of importing again and again, understood, we can import them at once and later use them many time however we want, understood. ]
[good, next do we need to use hive for app storage, like overlay should persists on app restarts. ]
[yo overlay assets did not persist after restart. I imported few overlay png in the assets tab, after restart they are all gone. we need to store them as well, create separate file for this and wire it]
[good, in project tab add new button for new project, this should clear the subtitle and audio (not assets) understood. ]
[good similar to the overlay tab, in the  lets in the text tab add subtab, for control and fonts, and we can store fonts there and also import custom fonts .ttf and .otf files understood]
[in the colors tab add text opaciyt, like something I want 0 opacity of text fill but only the stroke remains, so we need to control the text opacity  understood]
[for color lets add custom color picker as well, like a rectangular picker with HSL slider, Hex code input, rgba input. as well, create new widget for this color picker. to be used accoss got it]
[alright next lets add overlay animatons. create a separate file for this, and list them in overlay tab, understood, ]


===============version4==================
[alright right now lets add keyframe animations. so that we can animate text segement or overlay with keyframes based logics, understood, create new file for this and wire it. ]
[add indicator in the segment where keyframe is added, like a rhombus.]
[yo after adding first keyframe, I am unable to move the overlay position, then how am i suppose add the another keyframe to it]
[change the state of the add key frame button to remove keyframe when a already added key frame is there in that time, understood]
[good, add clear keyframe in the bottom control panel tab as well.]
[add subtab inside the keyframe tab for graph based speed controls. understood]
[also inside allow to create custom speed graph. by customizing the speed. if needed create separate file for this]
[good, but make the graph widget full width. and allow to add node point based graph control. where we can add intermediat nodes in the curve, understood]
[good, lets make the lower timeline control tab, horizontally scrollable  to add more buttons in it, first we add to switch on/off the each timeline visiblity, for the text and the overlay timeline, so what when we work with overlay we can turn of the text, and vice versa understood]


==========version5=============
[alright next this is new feature, I don't if this will work, right now for subtitle we are import json file which has time stam for each segment, what i want now is lets also allow plain text.  which can contains the texts as in paragraphs, so when its imported we break it to single word, and then place them in timeline with duration of 0.5s each, understood.]
[also allow to directly paste paragraph in the project setting paste your subtitles, where we can directly paste as paragraph. ]
[next lets add split button in the timeline control panel, to split any segment. also deleted button to delete any segment. understood]
[good, when we add new text. check if theres already segement at the play head, if then add next to new track, and don't directly add, ask a text input before adding, understood.]
[next for the audio, lets allow to extract audio directly from video, using image_picker we pick the video file and then we extract its audio and use that audio, create new file to handle this, we have ffmpeg so we can use that right?]
[next lets add merge feature, so that we can merge two consequitive segement words into segment,  like now two words will be in single segment with the same start and finish, understood, ]
[also add new split button for split to word for this type segment with multiple words. so that we can also spilt them into single word segments. got it]
[in the bottom text tab add method to change casing, like AA, aa, Aa]
[also the merging only working for 2, allow more than 2 as well but they should be consequitive. ]

==========version6=============
[awesome next now this is the crucial part, to get the timestamp based subtitles we need voice to text feature. like word level voice to text. what do you propose]
[lets implement whisper.cpp]
[i have ggml-small.bin file in my device storage, will it work]
[also I don't want tranlation, jsut phonetic transcription in roman alphabet]
[in  the project tab like add buttton to import model, and show the model ready as green when successfully imported. ]
[make two row grid layour now  in the project tab]
[we don't need devnagri characters. we only need phonetic transcription , not translation, we just need the romamized words. of those hindi word, understood]
[and why does it take too long time, the python script transcribe it within 30s, for the same, while our cpp code are running slower. its taking aroun 150s.]
[still not accurate but its fine, I can manually edit them, after the transcription is done lets show the transcription as plain paragraph. and json   format, so that we can edit directly from there as well instead of going through all the segment and correcting them individiually, this will make things faster. understood]
[good but it changed the segment text but the preveiw didn't change, preview is still showing previous text. ]
[continue and also have separate button for the to show  plain text and json ]
[ok but after I change a word in the text, the timestamp mapping broke and every segement were assigned equal duration,. we are changing the text content, not the timestamp understood. ]
[let change thing, lets not make the text editable, instead add new button where we show thing like text but kind of chip based like mapped to actual segment, so that we edit only that segment. showing it as king of paragraph but like suppose we tap on the word, we see a edit field below, something like that understood]
[and instead of dialog lets make in bottom sheet open, like we have for the custom color picker, got it]
[good, but separate the sheet for the json, and visual edit, like show them in seprate sheet. understood]
[another UX improvement that can we make top sheet modal instead of bottom sheet. like right now since we need to edit text the keboard shows up from bottom as well, and that pushes the modal sheet up. it feels too bad for UX understood]
[also don't close the key automatically unless the the apply to timeline or discard is tapped, don't close on every single segment change, got it]
[good, add a delete method as well. to remove unwanted word segments. got it]
[for the paste  and add text, as well let make top sheet modal, got it. ]
[good lets add a play pause button in the visual text editor, and hightlight the current text with green, so understood the plan. so that user can listen and see where is mismatch, got it]


=========version7========
[alright next lets add new timeline track, similar what we have for text and overlay timeline, I want a new timeline for background.  understood. create new files for this and wire it understood. ]
[I added the background from the background tab but its not showing in the   background timeline. like overlay does. so look into, I want similar to overlay.  understood]
[background assets storage like we have for the overlays]
[background segments should be collision proof as well like overlay and text segments]
[and there werent draggable, earlier like changing the track, or dragin in track was not working.]
[also though in the preview background is behind the text, in the output background is above the text layer. ]
[animated gif are not showing animations in the preview nor in the ouput. ]
[good next lets also allow video for the background. understood. ]
[Updated the Background Tab with a new Gallery (Videos) option.it only show Gallery(photos), for videos is not visible, like we have video picker in the project tab. for audio extraction we can use that picker that as well right. we don't need the audio video, just the video. understood, and it should be visible in the preview and ouput]
[video is not visible in the preview. check properly]
[we do have ffmpeg, if that can help even better way]
[good, but the background video is stucking/freezing/lagging, its not smooth. check on this, ]
[do we necessarily need do encode decode? check once if we are overdoing it]
[still the background video preview is not smooth, its lags a lot. check for reason]
[how do professional video editor handles these situation. ]
[yes to (Would you like me to try implementing a basic Hardware-Accelerated Decoder using MediaCodec to see if we can get even closer to that 'CapCut' feel?)]
[man so all this time weren't using any hardware acceleration anywhere? ]
[okay then what else can we optmize and improve with this, that we haven't done yet]
[major upgrades : Asynchronous Pipeline (Max Speed)I've switched the encoder to Asynchronous Mode using MediaCodec.setCallback., ]
[Cinematic Quality Profile Enabled AVC High Profile (Level 4). This prevents the 'pixelation' or 'blurring' often seen around sharp text edges in mobile video exports.]
[good, what else can we optimize. ]
[(1. Smart Text Texture Caching (Performance)), 2. Cinematic Motion Blur (Visual Quality) ]
[good next lets allow the video overlay as well in the overlay tab, just like we did for the video background, understood]
[disable the background preview window drag adjustment. we will adjust the backround only via the control slider from background tab. got it. ]
[also there is issue that if the audio is not imported the the backround or the overlay, or the subtile don't play, like the timeline doesn't move unless audio is imported. fix it. 
[if the background/overla video segment  is extended over its original duration then repeat loop. with a vertical dotted line in the segment where the original duration ends, understood]]


===========version8===========
[yo in the output the videobackground is smooth but in the preview its lagging a lot, and its even flickering sometimes even after we have paused. ]
[good lets add marking system, so that we can the audio and mark where each segment should be placed, for manualy timestamping. this would be better. like use hear to audio and tap that mark button, for like kind of beat marker but we do it for words. understood. so add a button in the timeline controller panel. for this and mark dot in the timeline ruler, ]
[I came across few toolss like silero and vad. since we have the audio and can also paste exact transcribed word as plain text. we don't need to transcribe right. then we can just force aligned the word based on the audio, can we achieved this? if then make proper plan, and new files to handle this]
[okay this is better than before, next add a new toggle switch in the timeline tools. for collision adjust, lie when drag edges of segment then right now if it colide other sement the same track it move to new track, so when this mode is on, instead of moving new track, we will adjust the other colidng segments width, understood, what i mean]
[next lets add new music&sfx timeline, similar to text, overlay, background, lets add a new timeline for musics and sfx, got it. to keep things organize and try to create new files intead of inlining codes in to single files. got it. ]
[in the bottom control panel, add new tab, similar to overlay, and background tab to manage music&sfx understood]
[I noticed that audio are playing sequentially, like I heard the timeline audion after the main audio ended, not when the playhead was that audio segment, man I want like other professional video editors, ]
[show the main audio in the timeline as segment in the first track of the audio timeline. got it]
[audio is breaking a lot]
[the audio tab import and extract from video is not working, they are not showing anything under the audio library after tyring to import or extract from video.  though the project tab  music and sfx import are working fine]
[no colision of audio segment should happen, add to new track, if theres already audio segment, ]
[and all the audio should be audible . like professional editor apps. understood. what i mean right now only the main audio was audible in the output]
[good, now few bug fixes and improvment, 1. limit the all(text, overlay, background, audio) segment till the main audio, no beyod that or before(0s) that, understood. ]
[show the add text(one we have in the project tab) in the timeline tool panel as well for quick access undeersood. ]
[allow assets renaming for the overlay, backgroun audio assets. ]
[allow pinch zoom in zoom out in the timeline. ]
[when zoom out to max that is 0 zoom, fit the timeline to full witdh. got it]
[move the stroke from style tab to effects tab]
[good, next add visiblity toggle for the audio timeline as well, like we have for text, overlay, backround in the timeline tool panel]
[also for the add text, make the default duration to be 0.2s for the segment understood]
[for stroke add the toggle switch like we have for text shadow and text abckground got it]
[also allow shadows, and strokes for the the overlays, understood. ]
[I don't think the overlay shadows or stroke are working. they are not visble, sliders change are doing nothing on them. ]
[okay here comes the issue, that the overlay put was a png with transparent background, but instead the main part shadow, the entire block is getting shadow that doesn't look good, so fix that same goes for the stroke, understood]
[shadow is working but the shadow blur on the overlay shadow isn't working as it is  in the texts]
[lets separate the font from the text to separate dedicated font tab. understood]
[add reset button to sliders. to reset to default. ]
[we don't have text blending mode right like the screen, dodge, burn etc, add that to text tab chip based not dropdown got it]
[a glitch in the background, that when I select the background segment it show difrent transformation, then when I deselect it. wtf is that]
[awesome everything is working now, only issue is that while preview auido and video are breaking a lot, though the output is smooth, the preview seems to be lagging if we can fix that, it would be perfect]
[still lagging, audio and video, and now even the text are not visisble]
[man preiview are not smooth, audio is breaking a log and video preview as well, whats wrong, how do professional editor handle the smoothness in preview]
[deepcheck again if there still some bottleneck or optimization for smooth preview]
[now the background video is missing from the output]
[good but man, the videob background is not at all showing up in the preview, it complete black background showing. what the reason. how do capcut handle this, or other profession editor handle this video background or multilayer timeline based  nested videos layer running so smooth]
[Would you like me to refactor our engine into this 'Background Buffer' architecture? It is a bigger change, but it's the secret to why professional editors feel so fast even with 4K video.
yes, if that more smooth and professional level then offcourse we  need that
]
[cool, now another issue is that playhead is not updating in real timetime, like when I tab in the ruler, the playhead indicator doesnt showp there, though when played, the played jump to there, so kind of a bugs]


======version9===========
[good now its done things are looking good, next lets add animation, in the style animations, lets add gradient wipe show animation, understood, ]
[next throwback animation. you know whats throwback animation?]
[wavy bending animation, for texts]
[awesome, next for the shadow, make the default shadow color black, and the blur 6, x,y offset to 8,8 understood]
[add quick alignment buttons in the postion tab. understood., like for quick postioning]
[by default keep the shadow switch off, also add shadow opacity control as well. and there seems to some bug with stroke and text background they are not showing in preview]
[good next lets add lock frame/playhead,  button in the timeline tool panel, what this do is lock a certain frame at playhead so that we can easily adjust the content at that played, understood. like we can ajust transformation effieciently. undertood]
[theres mismatch in the spacing and placement of elemens in the preview and output, the postion and spacing doesn't accurately match, check on this]
[still mismatching, like in the preview space bettween element looks smaller but in the ouput that same space is bigger,  and even size of text seems smaller in the output compared to the preview]
[alright assets/images/app_icon.png lets use this as app icon,]
[also lets add basic onboarding screens, create new folder for onboarding screen slides screen files. ]
[next lets add app_configs file to control thing from there, like I want to show some features right now, we need to hide them, we will do from the app config variables likein project tab, I don't to show the sub, words,voice, json,edit, model buttons, as of now, so we need to hide it for now, understood]
[add variable for all the buttons we so that we can controll visiblity of all the project tab buttons]
[good, next also add for the timeline tools  understood]
[lets remove the licensed fonts, lets not emebed it with the app, we will use only free fonts. users can import their own custom fonts. got it]
[good lets add a home page, profile page, and other few essential pages. ]
[yes a dedicated tutorial page as well]
[okay lets add another simple video player screen. where we import video with image picker. and then play it, a simple play controls like seek scrubber, play pause. mute, loop. and a preview canvas understood.  we will use native kotlin, complete separately so don't touch prevous files not even the mainactivity,  got it]
[good but to make more space for preview area, keep the play pause button on the left of the seek scrubber, and mute and loop and import  button on right on the the same row understood, ]
[lets add config to show this native, profile, tutorial and New project in the home screen]
[make the onboarding minimalistic as well]
[change the app label name to Typo Edit]

============version10==========
[alright next lets change the package name to com.typography]
[now lets sign it, for publish the app in play store we need to sign the release]
[do one final check the app is ready to publish right? also update the gitignore for key properties]
[update the apk artifact naming so that we get proper apk naming with app name and version instead of default app-release.apk got it]

======version11==============
[i got some reports that app is not compatible in some devices]
[we don't need camera permissions]
[in the postion tab. lets add new postion control, like a trackpad type of control from where we can control the postion of the selected element, understood, like sometimes when thing get too clutter in the preview draging element there can accidently move others, understood, what I am planning]
[good but the preview is kind of glitchy during thsi drag its not smooth, and also lets put this separate sub tab inside the postion understood]
[make the quick align button simple and minimal and compact.  not text just grid box, got it]
[alright next feature is big, lets save the project. so that we can store multiple projects, and can comeback and continue, we do have the dummy projects button in the homescreen,understood, design proper architecture for that, understood. shoud we use isar or hive or app storage or shared preference]
[good but theres bug that when I open the project the preview shows white, the text doen't showup in preview untill I do some changes in the background tab, ]
[allow project rename, delete, and duplicate]
[and in the recent project its automatically creating new projects, like I only started 2 projects but the recent project shows 5 projects. ]
[there's still bug that when I reopen the project the text isn't visible until I do some color shanges either in the background or the in the text color, look into this]
[okay next can we make the preview window adjustable, like we can drag the timeline control panel to adjust the height of preivew window, this was we can have control over how much the preview and the timeline track area takes. understood]
[cool but theres bug, the full view mode is shifting the the bottom control tabs from bottom to top. like I want them to be fixed at bottom understood]
[still no,  man the they should be fixed at the bottom of the screen always, right now the full view mode is lifting them up from the the bottom of the screen. understood what I mean]
[letss add glow , bending, reflection text effects. understood, what I  mean]
[bending isn't showing any effects on text]
[okay, next for the refelection when offset is 0, make them connected like undertsood]
[next theres issue that there  layer that is hiding the timeline behind it below, i think its the control tab min height area, can we remove that when no tab are open, understood what I am talking about]
[man something is causing a lot of audio breaking in the preview. whats the root cause, the same codebase was working fine then what could be the issue is it the device or what. look deep what might be the cause]
[so right now the preview audio is handled from flutter? will it improve more if we use native master clock?]
[do you want to look into migrating towards a Native Audio Master to get that professional-grade stability?: native audio master]
[working well so far, next make the timeline auto scroll when playing, like to follow the playhead, understood]i noticed how capcut does this, it keeps the play head at center and scroll the timeline, it looks smooth, understood]
[also another uX improvement when we play, and then I manually scroll the timeline, the pause automatically instead of playing again, get it,]
[good, theres some audio quality mismatch in the preview and ouput, sometimes glicth sound]
[in the audio assets library allow to play the preview, so that we can listen before adding it to timeline, got it]

===========version12==============
[next to streamline the editing, I want a feature to add bulk sound effects, like when a word appears on we need sfx so instead of manually adding same audio for each and every word, we can select the multiple word segments and then add the specific sfx in those time, in the audio timeline this will save alot of time right? understood the plan]
[add button in the timeline tool panel for this as well.]
[don't open filepicker, we will use the sfx from the audio library.]
[I am not sure of it but the volume control is not working effiently, like even with 0 volume they are audible, and even with volume  max(currently 2) they are hardly audible. ]
[good, next add a toast notification that sfx added succesfully. showing up from the top right of the screen, understood]
[also next lets use different color for different timeline, like separate colors for text segment, for background, for overlay, and audio segments. understood. ]
[use yellow ofr background segments, teal for audio, orange for text, sky blue for overlay, and lets have this color control in the project tab. projects settings understood]
[good next lets put the timeline label to the left most, like not as full separate row, just show it on the left most.  understood what I mean, this will give use more vertical space in the timeline area. ]
[don't fix it. it will just as the part of timeline track moves similar to the timeline when scrolled, just in the left most to the tracks undersood. ]
[good, next issue in the multi select, lets not allow cross timeline slection, like don't allow text and other segmement togethere, not like text and overlay segment are selected together, understood, so if we try to select other the segments then deselect the previous segment type, understood. ]
[add the default white background.]
[good but now the issue is that segment duration adjustment  drag is conflicting with timeline scroll man. this is bad]
[it working now, next clean up the unused track. like I drag segment to new track but again I move it back to orinal track then clean up the newly created tracks that are unsed or else they are unecessarliy taking up the timeline space, understood]

================version13==============
[alright next, the audio quality is soo poor, it like too different from original audio. kind of feels radio ]
[the bulk sfx toast notifier is not showing]
[next the postion drag adjust of elements in the preview window is not smooth, like not realtime, explain the reason, plan and then execute]
[default background white instead of black]
[next lets show the audio waveform in the audio segment, understood. how to achieve this]
[good, for the scale slider instead of having upper and lower limit lets have dial based scrubber, like I can swipe left to decrease and right to increase, from the current state, keeping the reset button as it is, understood, what I mean]
[in the keyframes lets add some preset keyframes, like I want one in which overlay moves from the left most to the right during the entire video. understood. so when i chose this preset, set the overlay duration from 0, to the end time. and add position keyframe to it, something like this understood]
[also add for diagonal pan]
[cool, but there are few bugs and improvement needed, first that the overlay started too late and  reached the end 5s before the actual video end. i don't want that, ]
[next I want update the y postion fion of this L->R overlay then how should I do that. ]
[good, but there issue in the keyframe adding, the end keyframe is outside the segment ends. why is that, and even the overlay reach the right end too early before the the video end, this doesnt look good understand]
[lets add overlay replace feature, like keeping the the same property and animation, just replace the overlay asset, understood. ]
[after successfull export show a success dialog showing the saved path with a preview button which opens that video, create new widget file for this understood and wire it properly]
[next lets add  assets management screen accessible from home screen where we can add our assets like overlays, sound and sfx, which we can use inside the editor. undertood. ]
[theres critical bug in the the background transfromation, like on scaling background in the background tab affecting the text, overlay as well. wtf is that]
[good next issue is in the rotation quick buttons,  lets keep only the increment buttons,  0 and +15, +45, understood. ]
[in the background fill mode new new buttons like scale to match the the height of canvas, and width of canvas, not streching, just scale, understood. ]
[next lets add custom aspect ratios as well. in the aspect ratio tab]
[save those aspect ratios, so that we don't have to fill it again and again]
[and man there issue, I set the aspect ration 1080x400, but still the ouput was 1080x1920, though the preview was correctly showing 1080x400]
[good, next in the home screen asset library add fonts management as well]
[in asset library audio sfx, allow to rename, play to listen as well. got it]
[in the overlay and background video picking is not working, only image working]
[allow gifs as well]
[good next in the colors, text colors lets allow gradient colors as well, understood, right now we only have solid colors. lets add method to choose/create gradient understood]
[yo the texts are not at all visble.]
[and in the home screen assets library for image and videos use image picker instead of file picker, just like we have done inside the editor understood]
[still text are not visible, nor the overlays, nor the backround]

[in the asset library fonts. remoce the Tt icon, remove the separate preview text and instead use the font name as preview text. ]
[inside editor the imported fonts card are not same as the in built font fix them, to use same card style]
[in the loop animation lets have control over over the animation speed, right they are animating too fast, I want slow animation, for some animations, so having speed control for these will good. understood]
[for the overlay animation as well]
[man how is this dial working, setting  loop speed minimum(100) is moving too fast, and loop speed 5000 is moving slow. man wtf is this logic, ]
[and I want more slower than what we have till now. ]
[the wavy bend loop animation is not working on overlay image]
[also lets add new ripple animation as well]
[good but there's some bug in this, that the bottom part of overlay is kind of showin up on top, whats that]
[okay next also lets have control for the intensity of ripple]
[reorder the timeline control tools, move the visiblity, marking, keyframe controls to last. understood]

===============version15==============
[okay next I want to improve the text postion layout, like when we have selected multiple word segment, and then stacked them, right now we are aligning them in single column right.  so lets make it more customizable lets have a new bottom control tab to set preset layout, like some cool grid based, bento grid style subtitle positioning, okay,  understood the plan?]
[good next lets add more layouts, ]
[also lets allow users to create and save layout as their presets. ]
[cool, next right we are breaking everything into word, lets add new feature to break subtitle into sentences, undertood.  like in the paste subtile lets add feature and new button one for breaking into words, and another breaking into sentences, for sentence like each line as one segment, understood the plan. ]
[awesome next  Persistent Text: The text you pasted in the input field will stay there. If you open the "Paste Subtitles" dialog again, your text will still be exactly as you left it. This means you can quickly swap between "Words" and "Sentences" without ever needing to re-paste your paragraph!]
[good next, now that we can have a whole sentence as one, till now we have the divide method that breaks sentece into words, and stack method that stack word segment into stair case in timeline,  now I want a new button that combines this two, that we can apply on the sentence segments.  this way we can havel all the words in the sentence in one frame posible, understood. ]
[gives every single word the exact same start and end time as the original sentence,  no we don't need this use the just like divide does, but keeping the overall duration of each word segment within the sentence duration undestood
[a quick improvement while we break text into sentence instead auto aligning them in sequentialy in timeline, lets stack them in the in separate track understood, this was users can comfortably drag the sentence segment to the right time and do the further process, understood, only for the sentence not for word understood the idea]
[okay more imrovement keep the sequential like before but in seprate track]
[next when drag segments, beyond the screen size auto scroll the timeline, or else I am having to drag and scroll and drag again and scroll to right place, undertood the problem]
[also alllow multi segment drag as well, when multple text segment are selected and dragged. got it]
[critcal issue, segments are disappear from the track after drag, ]
[still segment is getting lost, but when another segment is dragged previous one show up and next one dissapears]
[okay, but while dragging multiple segments, during the drag only one segment is visible others are not, that is bad if we can't see where other segment will be placed. understood]
[yo why its shrinking height though, show it just like the single segment drag works, ]
[no keep all the segment similar not shrinking for the ghosts as well]

===============version16==============
[okay next improvement for the video export preview, use device videplayer insead of external video player, right now its opening in MX player. ]
[add a clear button in the paste dialog to clear the input field. ]
[in the styles tab why is animation tab button taking more space that the collor tab button]
[export preview is black, spinning endless]
[good, next improve the video player design, keep it monochorome, minimalistic., and also the successfull export dialog, and show the user understanable path instead of content://.....]

[okay, next lets keep the playhead fixed at center and the timeline move behind it, like capcut does understood. ]
[good but the track label are on the right of the 0s, fix that. it should be on the left of 0s start got it]
[good make the scrubing more smooth, right it sometime lags a bit]
[alright next in the custom aspect ratio, show the ratio as well. make it dynamic as user input vaule of height and width.  got it]
[next show the selection box in the preview window, like which element is selected. got it lets remove the direct drag method that we have right, first select and then drag, understood. ]
[remove the corner dot and color from the box]
[while draging element the box should follow it in realtime, right it not]
[remove pinch to scale method. ]
[lets use pinch to zoom in out the  whole preview so that we can work more precisely understood]
[so remove it from the timeline control panel]
[the selection box widht is too much kind of its same of all length of texts , it not wrapping the text properly]
[and the box is staying in the preview even when the selected text is not in the current frame. ]
[the progress time is not updating while scrubbing]
reduce the selection box stroke width, make it thinner just visible, and chose grey color for it]
[when multiple text are selected and then wrap all in one box, understood. ]
[there is some bug that even being under the playhead, sometime the text are not visible in the preview. specialyy when stacked. ]
[previously we had that swipe scrubing are below the preview area whe full screen mode, lets remove that and also the full screen mode, since we now drag and adjust the timeline area height. understood]
[awesome there issue in the selection box for overlays. its not bouding the overlay properly]
[also overlay are not visible right after they are added, like they are visisble only when some changes like transformation or animation is applied]
[good next issue in the volume control of main audio, its not workig. even after setting volume to 0 its audible and  even on maxing its alway is same volume, just increasing volume is just breaking the audio quality. ]
[okay, next while app opening there is weird glitch sound, what is that]


===============version17==============
[alright next lets add custom paletes, like users can create custom color palete for there project so that they quick access the colors they want in the project. understood.   we can add new paletes from the asset library in the homescreen, understood. ]
[good but show these palettes in the main color tab not inside the custom color picker sheet. ]
[lets add extractfrom image feature as well for the color palettes]
[good but it stilll missing important colors from the image, so also allow manula review and pick color from image understood]
[nah, its how do I add the picked color, also add the offset on where I drag and where the picker is so that I can see wchich color I am picking right now it getting under my finger, so I am unable to pick color precisely understood]
[lets have separated sub tab for color palette in the style tab,  and keep that as defualt tab. understood. ]
[also for colors use square box instead of circular]
[good lets add some presets color palettes as well in the palette library, create new file for this or else the aset libry file will be huge, we will have, huge range of color palettes presets understood. like for all shorts of major colors. think of its a ssub app for color palettes]

===============version18==============
[fixing the background issues]
[looks like its working now, but lets not allow extend the backround beyond the main audio/text duration, understood]
[show the background assets resolution in the background control for the selected bacground segment]
[now lets improve the scale to height and scale to width quck button for fill mode, for example I imported an background which has resolution of 675x1200, right now its in potrait mode when I imported, but since I am on the 1920x1080(landscape aspect ratio) i rotated the image, so now the image temprory resolution become 1200x675,  now when I press the fill mode scale to width, I want to scale the background with its center as anchor such that the new width of background matches the project widht(1920), no streching or squahsing, understood, and similar for scale to height, should scale such it matches the height of project(eg here 1080) got it]

[and if there are any empty tracks then also add a - button to remove that track(only if its empty) got it]
[good but buttons design are not good and not in center aligned with tracks]
[keep the +-  button in row when both are present instead of stacking them in one column got it]
[good, but by default alway show an extra track for each so that we can drag segement to it]
[a ux improvement, while importing audio it alway device audio instead of what was the last path I selected, that annoying that I had to browse to that path again and again]
[always show the track type label even for empty tracks, got it]
[and also the + button as well]

===============version19==============
[good next in the paste dialog, add mini play pause and progress bar scrubber for the main audio, so that we can replay and write accurate transcription, understood. ]
[good, next lets remove the gradient design of the segment UI in timeline, keep solid colors. got it]
[also remove the glow effects in]
[remove corner radius as well]
[in the track always show the +- button but if the track is not empty disable the - button, and users press then show that tracks not empty understood]
[okay next, lets add method to save the project to device as well so that we can import project even after app is uninstalled, understood, in the homescreen project add this method. understood. ]
[what about assets used]
[lets add new text entrance animation. like smooth slide up. like letter by letter, like right most moves up first and so on. understand what I mean]
[lets add dots in the selection box vertex, which we can use for scaling element on draging, undertood. ]
[now lets also add rotation handle as well. show this in the top right corner, got it]
[yo rotation is kind of working on oppsite direction]
[good, next in the past subtitle dialog lets add toggle switch whether to set the sentences in staggered sequence in different track or in  sequence the same track, understood]
[good next lets add a junction drag controller this will work at the joining section bettween to segment, like suppose two segment are adjacent, common dynamic trimming of both segment, how should we proceed, ]
[in the app config for whether to show the export/import project ]
[also for the asset library in home screen, timeline colors in project tab]
[in the preset color palette instead of random color palette add premuim color palettes that looks premium and luxury, understood]
[keep the color order like this, for background, main text, and subtexts]
[next lets add zoom and pan mode. add this in the zoom slider row of timeline panel, what this will do is this allow to zoom and pan the preview window without allowing any element selection in the canvas, understood. ]
[pan isn't working properly add debug logs]
[good but when paning is toggled off the preview gets reset I don't want that since I want to edit the element select them now, so add separte reset button next to it got it]
[good now can we optimize and remove this pan toggle directlly allow pan when dragged from empty space, and to be safe lets add a toggle for this mode in the project tab, for whether to show the pan toggle, understood so that we can keep both method understood]
[good next lets add new text animation, right we do have slide up and slide down animation, similar to that I want animation which slides up and down from the bottom edge and top edge respectively understood]
[when applied on sentence segment then show this slide animation individually for each word understood. ]
[okay theres a critical bug in split segment its not  spliting at playhead check once]
[okay, next lets add few important screen that a professional industry standard app should have. understood]
[in the export screen show the human readable path instead of content:// ...]
[some animations are showing up in the animation tab but not in the styles animaiton subtab for texts, look into this]

===============version20==============

[alright next can we refactor the animation logic separately like to have separate file for each animations so that its organize for future more animations for scaliblity. or else if we put everything at once place if something breaks all the animations will break right, and also the inline code will keep increasing uncessaryly to understand in future right? understood, so plan accordingly ]
[yes, I don't want animation evalutator to be monolithic since we will add more complex animation in future so keeping it and handling through signle file will be hell later on right?]
[good, but it would more better we have separate file for each animation preset right?]
[good, next lets have control for segment duration add new control tab for this]
[yo its kind of freezing the slider, and other increment and decrement button after one change, and also the changes are not being realtime like its only seen after the I try to scrub timeline, ]
[good, but there is collision/overlapping happening when adjusting with slider, end slider sensitivity is too high, also get out of focus from input field when tapped outside. got it]
[good, this tab show the track number as well, and control to move to different track that are free at that duration, understood. ]
[lets have separate subtab for the timing and track in this tab]
[improve the splash screen with kinetic typography animation. and intro with proper tag line. understood]
[advance motion graphics style kinetic typography understood. ]


===============version21==============
[add focus mode looping feature where we can add start and end time and focus on only that time in the timeline]
[in the project tab add save project method to manually save progress as well, understood]
[in the timeline toobox panel keep the delete button next to split and merge button got it]
[also the stack and reset button next to divide and burst button]
[when we use divide, burst on a segment, then keep the child segements selected by default understand. ]
[next right now the selection box is uneccessarily too big compared to the actualy element area. check on this]
[still the left and right edge of selection box is far from the actual segments left and right end]
[still no, man there is big horizontal space between the selection box left and right edge and the element left and right end. the box calculation too wrong]
[good next lets add more cool entrance animations. understood. think of new animations style that we can achieve but we are missing right now]
[spiral drop and glitch animation are horrible, wtf are those animations. the don't look cool at all, they look ugly as hell]
[lets intergrate 3d animations as well, we can achieve those as well right with opengl]
[Your app does not support 16 KB memory page sizes. Learn more---> got this in play console]

[next in the asset library lets add feature to directly import all the assets like background, audio, sfx, fonts, overlay, like I will have them organize inside subfolder of folder with specific name, then we upload master folder and the app organize the the assets in the asset library accordingly understood.  create separe file for this service]
[for the permission shouldn't we use the system dialog? rather than getting redirected to permission setting?]
[so is this kind of critical permission right? it might slow down the app review processes. I wanted this feature just for myself not for other users. ohters can import all of the assets individually got it ]
[not necessarily in debug but also in release apk but not in aab, can we do that]
[okay next in assets lets have separate tabs for background images, and background videos, overlay images and overlay videos, music and sfx, understood]
[in play console still getting this Your app does not support 16 KB memory page sizes. Learn more and Users that have the APK with version code 3 may need to accept the android.permission.INTERNET permission, which may result in them not upgrading to this version of the app.]



===============version22==============

[now that we have everything to create kinetic typography, we can completely automate the typography videos, like auto layout, colors, font size,scale, animation type,  etc, on one click. like suppose I am timed the sentence segment properly in the timeline, then after that when we automate it uses burst+ stack on the each sentence segment. and then randomly scales, use different font and font size, colors for each text segment, align them in cool layout, wihout collision or overlapping. animations, right this will be cool way to move to advanced kinetic typography with minimal effor right? lets plan this properly and design and implement this, idea, create new files and folders to avoid increasing inline codes if that can be done, to keep things understanble and organize, understood]
[awesome but, use the burst and stack on individual setentence segment, and keep the those child segment within that duratiion only, udnerstood, ]
[and also the background color as well based on the palette, understood, right now we arent updating backgroun layer i guess]
[also allow user to choose the background, and text colors. instead of forcing style preset]
[the designt of the Auto Kinetic sheet is not good, and man have separate file for this widget instead of inlining in the time lineeditor file]
[still layout sucks, man wtf is that, for the color selection user horizontal scrollable selectors, the style overrides toggle use separate row and no card wrapping, use kleeone font for the UI font. instead of bold poppins got it]
[for color allow custom color like we have in the styles tab colors. ]
[allow to create cutom color and font presets, ]
[allow updating and deleting, renaming the  custom preset style]
[also create new layout style preset for blocky layout like compact but like bento-grid style layout in the kinetic sheet understood. ]
[still its doesnt feels like compact, I want layout such that the bouding box share edges, like compact. understood]
[good but text are ovelapping, I don't want that to happen. collision calculation issue, the selection box calculation is not like beofre the the kinetic process, look into this]
[also there is another bug, addition color are appearing for text aside from the selected text color]
[okay next lets also have toggle for enabling and disabling entrance and exit animations]
[good, another improvement, that things are wrking but right now all the segments are kind of getting in separate track, making huge number of tracks stacked, understood, like UI UX feel off. so like it should happen like this, like first sentence is burst and stacked, if first sentence has suppose 5 words then they will be stacked in the 5 tracks, then if next sentence has 3 words then use the exisiting tracks, if the words 7 then use the existing 5 plus add 2 new tracks something like this, instead keep creating new track for every words, understood. ]
[good but invert the order like we use in the stack mode, like first word be at bottom and last word to be at top, understood]
[awesome now, lets add more style preset layouts. propose some cool layouts for kinectic typography]
[and we also already have many layouts in the layout tab like stairs, wave, create kinetics style preset for those as well. undeerstood]
[good but lets not integrate colors, stroke, glow, fonts in the style preset, we can manually select color from the color selection area, understood, the preset will only of the layout understood]
[good next by defualt turn of the exit animations, stroke, glow]
[allow mixed layout selection]
[lets also choose the entrace and exit animation types. for mixed animations styles instead of using same animations, understood]
[good but you removed the toggle, lets keep the enabling and disabling toggle.  got it]
[also lets have control over the minimum and max font size. got it]
[good next we need control over the rotation as well whether to allow random any rotation, no rotation, or only just 0, 90, 180, 270, understood. ]
[and few texts are overlapping after rotations.]
[control for min and max scale of texts]
[good next there is issue when reapplying the kinetic effect, like suppose I select different layout after applying previous, then its not showing chnnges, like still showing previous layout, and also the previous color settings. look in to this]

===============version23==============
[okay next lets integrate admob ads, for now only app open ads and interstitial ads on exporting, understood.  create proper files for ad service, adconfig, adhelper, etc thats are requried to better scaliblity and organized ad system understood]
[yo man improve the onboarding screen man it look horrible low quality onboarding. very low quality design]
[have we integrated in app update, and in app review? if now then do it]
[add enabling and disabling boolean vairable control for ad units in the ad config]
[alright next in the color palette system, I want few improvement in the system, like label the colors in the palette for background, main text, sub main, and normal text understood. for each palette understood]
[also during the adding new color palettes in the asset library as well. got ]
[also can we export and import color palettes so that we don't loose and be able to recover or color palettes after uninstalling and reinstalling or like someone shared their color palettes, create new service file for this and plan this feature ]
[in the color palette extract from image, we need update thing like don't auto choose colors, instead allow user to select for each colors manually like for background, main text .... etc understood]
[for add palette method as well]
[good next let use this color palettes in the auto-kinetic typography feature, instead of random colors we were using earlier, understood]
[show both the background and text color selector together in the kinetic sheet. as palette, with labelled, understood]
[okay next I want to control the height of segment/tracks, add this feature in the project tab in the preview settings. undertood]
[good and next lets use kleeone text for the timeline UI, segment texts etc understood]
[okay next in the background tab, when we add a backround, whether solid, images asset or video background, add a method to extend this background to full duration like  start to end, like sometimes I need only background full, so add this method so like after we added a background layer in the timeline, once we select that and press this new method, it extend to full duration and if theres any other segment in that track move them to empty backgrounds tracks or to new tracks understood the plan]
[good but theres issue, though the segment in the timeline is extended to full duration its not showing the background for the full duration, so there is some lag/bug in the updating the preview background, understood]
[ even though the apply neon glow, outer shadow are turned off, I am still seeing some text shadows why they appearing? check on this]
[also for the stroke, I see strokes even after disabling them]
[remove stroke from all the style presets, I am still seeing some stroke gett applied from some style preset, we will mangage stroke from the toggle we have got it]
[same for the shadow and glow, ]
[good next lets add few adjustment setting for background assets and overlay assetss like brightness, saturation, blur etc understood. ]
[next update the effects tab's subtabs make them horizontally scrallable so that they fit properly instead of getting new lined understood]
[okay next in the home screen tutorial  screen create more tutorial titles related to our app feature so that I can start creating the video for those topics and upload them to youtube and then we can use those url in this tutorial, and so that users can watch them, we might need to use youtube embeddeding url system for this. plan and implement this got it, instead of opening youtube we can embedded the yt video in the app right? we can do this purely on dart as well right ?]
[more tutorial titles]
[good but right now there are some copyrighted thumbnail showing up, remove them untill we add real video urls, don't use some dummy yt videos got it]
[good, next I want some new features, in home screen add a more button that show a new more tools screen, we will add some other tools there understood. ]
[okay now I want to add a tool that makes image sequence videos, like I will import few images, will import audio(optional), set duration, control whether to use image randomly or in sequence, set aspect ratio, etc control understood, create separate folder for this new tool feature these are just additional tools, got it, this should be kept separately from out typography editor understood, like a separate side project understood]
[good, but save the output in the public directory, update mediascanner, ]
[also show the preview after export like we have in our kinetic project]
[alright next update the layout for the image sequence control, use the bottom control tab style layout similar to out main kinetic project, understood]
[good, but the preview and the ouput resolution are not matching, or I will say, in the ouput the image are not properly centered, scaled to match the ouput resolution, i am seeing black spaces in the ouput, also update the image sequence preview window design, just like we have in the main project, understood, ]
[no corner radius is required,  don't nested wrap things in preview, minimum interval should 0.01, add numerical input method for the image interval as well]
[remove the the per image progress bar, and that image x of y  UI, ]
[add total duration control, by default 8s, if audio is imported then use audio duration,  also keep the image interval duration, input field in separate row got it]
[and use audio extraction from video feature as well like we have in the main project, so that we can import audio directly from a video, and use that understood]
[preview is working but in the ouput, the video is black, image sequence are not visible]
[good, next make 16:9 as default aspect ratio, cut as default transition, 0.5s a default image interval, and there is some issue in the total duration calcution in the export tab understood]
[show the video progress bar, scrubbable, play pause, seek features]
[good, next remove other tools, from the tools screen they are useless as of now, we will integrate more tools later on.  understood]
[okay, next similar to the image sequence let add another tool, in this we will add a text, we will change font, like same text but font sequence, understood,]
[use fonts from assets library, ]
[use the control layout similar to the image sequence]
[list all the avaiable fonts as chip, similar to how we have in the main project. ]
[but in the export tab, the compile video loop button is not clickable, due to the long summary list, pusing the button beyond screen, ]
[in fonts tab add select all button to use all the avaiable fonts, also no need for the ordering of fonts. so no need for separate active play sequence row of font, understood]
[just add a first font selection method, like we can chose only first font, and rest other can be random. ]
[random but from the selected fonts man, like a separate selection for the first font and random from the selected fonts understood]
[also there mismatch in the preview and output of the font size, like in text size is bigger but in the otput its too small]
[good but create a subtab for the first font selection and the others, just like we have in the main project tabs,  understood]
[good, next lets remove the native player thing, we don't need that anymore in the homescreen, undestood]
[next, now that we already using bottom navbar in home screen, let move the asset library, tutorial, more tool to navbar, keep only new project and import project in homescreen understood]


===============version24==============

[okay next in the more tool I want a new tool for creating image, with text overlay, user uuploads and image, we add text on it, postion, font, color size etc controls, keep the control UI similar to other two tools, bottom control tab type, understood]
[save this image project, inside this tool so that we can re-edit them got it]
[we don't necesarily need text background, and save the image to public directory, like the other tool ]
[where is it getting save I don't see it in gallery.]
[okay next for the UI lets use kleeone font on this screen, got it]
[okay next add shadow x,y offset slider control, shadow color, shadow blur,  keep the label and slider in different row so that slider get full width, understood]
[yo I still don't see the image in the gallery, and where is getting saved, ]
[show the preview after export.]
[for the colors, allow custom color picker as well, the one we used in the main project]
[okay , I want the output to be 1080x1920, for 9:16, 1920x1080 for 16:9, 1080x1080 for 1:1, got it, ]
[also for adding text allow direct input from the text tab as input field, intead of necessarily using the double tab, understood]
[add letter spacing control slider]
[for the effects add toggle for the enabling stroke, shadows understood]

[okay next lets add real ad unit now, this an interstitial ads  use this ad unit ca-app-pub-5829296907403264/8997941664]
[ca-app-pub-5829296907403264/6196518507 use this rewarded ad for typography export reward, got it]
[name this ad unit specifically as typo-export-reward-ad, ]
[next image-text-inter-ad, ca-app-pub-5829296907403264/6004946814 for exporting image text from the more tools image text screen]
[image-sequence-export-ad, interstitial ad ca-app-pub-5829296907403264/2833885779]
[font-sequence-inter-ad, ca-app-pub-5829296907403264/2111831047 for font sequence export]
[app-open-ad, ca-app-pub-5829296907403264/5047088367 for app open ads]

[good, next in the project tab, for extracting audio from video, lable it Extract from Video, instead of simply Video, its kind of misleading otherwise got it]
[similarly for new project make it "New Project", for save project "Save Project got it, for audio, "Main Audio"]
[for the Kinetic in the timeline toolbox,  use "Auto Ai" this way users will think its the AI doing stuff]

[there's some issue with app open ads, its appearing randomly even while actively using app. wtf is that, shouldn't it appear only during app opening]
[in the text edit dialog(visual editor) of project tab show the scrubale miniplayer similar to what we have in the paste dialog understood]
[good but the highglight is  not being realtime, as we scrub or as audio is played]

[awesome next update and redesign the onboarding, showing the kinectic layout text layouts animations showcase, for like masonary, blocky, stair, layouts animations, understood, this way users can understand what the app is capable of, of compleletly redesign the onboarding flow, understood]
[wtf is this gooffy looking onboariding, its soo ugly as hell, told yo you  to completely redesign the onboarding flow from scratch. ]
[keep it minimalis, use muted color palettes, use font variation, scale variations, just like we did in kinetic typography]
[good but fuck those nested card based design. and improve the text content]
[good but text content need improvement, like for the first screen, "Create trending typography Edits", for second "Grow your social media page", third "Seemles typography editor"]
[yo not the title but the in the actual kinetic typography. understood]
[theres issue that on the first time opening app, the bottom navbar are not visible in the home screen]
[there is still issue in the app opening ad, its showing up, even without app start, or app open, what the main cause, of this, ]
[okay next, in our main typography project I am still seeing thin text strokes in the output, they are not in the preiview then why are they appearing in the output. check on it, ]
[and the quality issue, like the texts doesn't look sharp, crisp, looks like low quality, why is it so]
